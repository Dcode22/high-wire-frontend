import { CheckUpvotedPipe } from './check-upvoted.pipe';

describe('CheckUpvotedPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckUpvotedPipe();
    expect(pipe).toBeTruthy();
  });
});
