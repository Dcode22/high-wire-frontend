import { CheckDownvotedPipe } from './check-downvoted.pipe';

describe('CheckDownvotedPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckDownvotedPipe();
    expect(pipe).toBeTruthy();
  });
});
